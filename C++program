#include <iostream>

int main() {
    int n;

    std::cout << "Enter an integer: ";
    std::cin >> n;

    std::cout << "Even numbers from 0 to " << n << ":\n";
    for (int i = 0; i <= n; i += 2) {
        std::cout << i << "\n";
    }

    return 0;
}
